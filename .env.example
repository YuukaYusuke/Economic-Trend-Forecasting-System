# Salin ke .env untuk pengembangan lokal (jangan commit .env)

# Opsional — kurs pasangan tunggal (Alpha Vantage). Tanpa ini, Frankfurter tetap aktif.
ALPHAVANTAGE_API_KEY=

# true = boleh latih model saat buka app (lokal). Di Cloud set via secrets.toml.
# ALLOW_RUNTIME_TRAINING=true
